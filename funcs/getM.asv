function [Me] = getM(Ti, Te, Mi, gamma)
    % Calculates New Mach Number
    syms Mo
    T0 = Te * (1 + ((gamma - 1) / 2) * Mi ^ 2);
    T0star = Ti * ((1 + gamma * Mi^2)^2 / (2*(gamma+1)*Mi^2));

    Tstar = 

    T_ratio = Te / Tstar;
    Mfunc = (((gamma + 1)^2 * Mo^2) / (1 + gamma * Mo^2)^2) == T_ratio;
    Me = double(solve(Mfunc, Mo));
end